rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 開發測試模式 - 允許所有讀寫
    // 這個規則適合開發和測試使用
    // 生產環境請記得更新為更嚴格的規則

    match /{document=**} {
      allow read, write: if true;
    }
  }
}
