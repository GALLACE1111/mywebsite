# 🔧 部署故障排除指南

## 🚨 雙平台部署失敗

### 立即診斷步驟

#### 步驟 1: 執行診斷工具
```cmd
雙擊執行: 診斷部署問題.bat
```

這會測試：
- ✅ Git 狀態
- ✅ Nuxt 構建
- ✅ SSG 生成
- ✅ Firebase 登入

---

## ❌ 常見錯誤和解決方案

### 錯誤 1: Firebase 部署失敗 - "Error: Failed to authenticate"

**原因**: 缺少或無效的 FIREBASE_TOKEN

**解決方案**:
```bash
# 1. 在 CMD 中執行
cd D:\網頁\website
firebase login:ci

# 2. 複製生成的 Token

# 3. 前往 GitHub
# https://github.com/GALLACE1111/mywebsite/settings/secrets/actions

# 4. 檢查 FIREBASE_TOKEN
#    - 如果不存在，創建新的
#    - 如果存在，更新它

# 5. 重新觸發部署
git commit --allow-empty -m "test: 重試部署"
git push origin main
```

---

### 錯誤 2: GitHub Pages 部署失敗 - "npm run generate failed"

**可能原因**:
- Nuxt 配置問題
- 依賴缺失
- 構建錯誤

**解決方案**:

#### 方法 1: 本地測試
```bash
cd D:\網頁\website\frontend-nuxt
npm install
npm run generate
```

如果失敗，查看錯誤訊息並修復。

#### 方法 2: 檢查 Nuxt 配置
確保 `nuxt.config.ts` 支援 SSG：

```typescript
export default defineNuxtConfig({
  // ... 其他配置

  // 確保沒有這些會阻止 SSG 的配置
  // ssr: false,  // 這個要註釋掉
})
```

#### 方法 3: 簡化 workflow
臨時使用只構建不部署的版本來測試。

---

### 錯誤 3: "frontend: Cannot open: No such file or directory"

**原因**: workflow 還在引用舊的 `frontend` 目錄

**解決方案**: 已修復，確認 `static.yml` 使用 `frontend-nuxt`

---

### 錯誤 4: "Process completed with exit code 2"

**原因**: 構建或部署腳本執行失敗

**檢查步驟**:
1. 前往 GitHub Actions 查看詳細日誌
2. 找到紅色 ❌ 的步驟
3. 展開查看完整錯誤訊息

**常見原因**:
- 依賴安裝失敗 → 檢查 package.json
- 構建失敗 → 檢查代碼語法錯誤
- 權限問題 → 檢查 Secrets 設置

---

### 錯誤 5: "Error: No url found for deployment"

**原因**: GitHub Pages 未啟用

**解決方案**:
1. 前往: https://github.com/GALLACE1111/mywebsite/settings/pages
2. **Source**: 選擇 "GitHub Actions"
3. 保存設置
4. 重新觸發部署

---

## 🔍 查看詳細錯誤

### GitHub Actions 日誌

1. **前往 Actions**:
   ```
   https://github.com/GALLACE1111/mywebsite/actions
   ```

2. **點擊失敗的 workflow run** (紅色 ❌)

3. **點擊失敗的 job**

4. **展開失敗的步驟** 查看完整錯誤

5. **複製錯誤訊息** 以便診斷

---

## 🧪 測試本地構建

### 測試 SSR 構建 (Firebase)
```bash
cd D:\網頁\website\frontend-nuxt
npm install
npm run build
```

**預期結果**:
- ✅ 構建成功
- ✅ `.output` 目錄已創建
- ✅ 沒有錯誤訊息

### 測試 SSG 生成 (GitHub Pages)
```bash
cd D:\網頁\website\frontend-nuxt
npm run generate
```

**預期結果**:
- ✅ 生成成功
- ✅ `.output/public` 目錄有 HTML 文件
- ✅ 沒有錯誤訊息

---

## 🔄 逐步部署測試

### 步驟 1: 只測試 GitHub Pages

**臨時停用 Firebase workflow**:
```bash
cd D:\網頁\website\.github\workflows
mv firebase-deploy.yml firebase-deploy.yml.disabled
git add .
git commit -m "test: 暫時停用 Firebase 部署"
git push origin main
```

**查看結果**: 只有 GitHub Pages workflow 會執行

### 步驟 2: 修復 GitHub Pages 後啟用 Firebase

```bash
cd D:\網頁\website\.github\workflows
mv firebase-deploy.yml.disabled firebase-deploy.yml
git add .
git commit -m "test: 重新啟用 Firebase 部署"
git push origin main
```

---

## 📊 檢查清單

### Firebase 部署檢查

- [ ] 本地已登入 Firebase (`firebase login`)
- [ ] 已執行 `firebase login:ci` 獲取 Token
- [ ] Token 已添加到 GitHub Secrets (`FIREBASE_TOKEN`)
- [ ] Token 名稱完全正確（區分大小寫）
- [ ] Nuxt 構建成功 (`npm run build`)
- [ ] `firebase.json` 配置正確

### GitHub Pages 檢查

- [ ] Repository Settings → Pages 已啟用
- [ ] Source 設為 "GitHub Actions"
- [ ] SSG 生成成功 (`npm run generate`)
- [ ] `.output/public` 目錄有內容
- [ ] workflow 文件路徑正確

---

## 🆘 需要幫助？

### 提供以下資訊以便診斷：

1. **GitHub Actions 錯誤截圖**
   - 失敗步驟的完整日誌

2. **本地測試結果**
   ```bash
   cd frontend-nuxt
   npm run build      # 結果？
   npm run generate   # 結果？
   ```

3. **Firebase 狀態**
   ```bash
   firebase login:list   # 結果？
   ```

4. **GitHub Settings 截圖**
   - Secrets 頁面（確認 FIREBASE_TOKEN 存在）
   - Pages 頁面（確認設置）

---

## ⚡ 快速修復命令

### 重新安裝依賴
```bash
cd D:\網頁\website\frontend-nuxt
rm -rf node_modules package-lock.json
npm install
```

### 清理構建緩存
```bash
cd D:\網頁\website\frontend-nuxt
rm -rf .nuxt .output node_modules/.cache
npm install
npm run build
```

### 重新獲取 Firebase Token
```bash
cd D:\網頁\website
firebase logout
firebase login
firebase login:ci
```

### 重新觸發部署
```bash
cd D:\網頁\website
git commit --allow-empty -m "test: 重試部署"
git push origin main
```

---

## 📞 快速連結

- **GitHub Actions**: https://github.com/GALLACE1111/mywebsite/actions
- **GitHub Secrets**: https://github.com/GALLACE1111/mywebsite/settings/secrets/actions
- **GitHub Pages**: https://github.com/GALLACE1111/mywebsite/settings/pages
- **Firebase Console**: https://console.firebase.google.com/project/side-project-663de

---

**請先執行診斷工具，然後告訴我具體的錯誤訊息！** 🔍
