# 🚀 完整部署指南

## 📋 部署前準備

### 1. 確保已安裝必要工具
- ✅ Node.js (v18 或更高)
- ✅ Firebase CLI
- ✅ Git

### 2. Firebase 專案資訊
- **專案 ID**: `side-project-663de`
- **GitHub**: https://github.com/GALLACE1111/mywebsite

---

## 🔥 Firebase 部署

### 方式一：使用自動化腳本（推薦）

#### 部署前端（Hosting）
```cmd
部署到Firebase.bat
```

這個腳本會：
1. 檢查 Firebase 登入狀態
2. 構建 Nuxt 專案
3. 部署 Firestore 規則
4. 部署 Storage 規則
5. 部署 Firebase Hosting

#### 部署後端（Functions）
```cmd
部署Functions.bat
```

這個腳本會：
1. 檢查 Firebase 登入狀態
2. 安裝 Functions 依賴
3. 部署 Firebase Functions

---

### 方式二：手動部署

#### 步驟 1: 登入 Firebase
```bash
firebase login
```

#### 步驟 2: 構建 Nuxt 專案
```bash
cd frontend-nuxt
npm run build
cd ..
```

#### 步驟 3: 部署 Firestore 規則和索引
```bash
firebase deploy --only firestore
```

#### 步驟 4: 部署 Storage 規則
```bash
firebase deploy --only storage
```

#### 步驟 5: 部署 Hosting
```bash
firebase deploy --only hosting
```

#### 步驟 6: 部署 Functions（可選）
```bash
cd functions
npm install
cd ..
firebase deploy --only functions
```

#### 一次部署全部
```bash
firebase deploy
```

---

## 🐙 GitHub 部署

### 提交並推送變更

```bash
# 加入所有變更
git add .

# 建立提交
git commit -m "描述你的變更"

# 推送到 GitHub
git push origin main
```

### 查看 Git 狀態
```bash
git status
```

### 查看提交歷史
```bash
git log --oneline -10
```

---

## 📂 專案結構

```
website/
├── frontend-nuxt/          # Nuxt 3 前端
│   ├── .output/           # 構建輸出（部署到 Firebase Hosting）
│   ├── components/        # Vue 組件
│   ├── pages/            # 頁面路由
│   ├── stores/           # Pinia 狀態管理
│   └── nuxt.config.ts    # Nuxt 配置
├── backend/nodejs/         # Node.js 後端（開發用）
├── functions/             # Firebase Functions
│   ├── index.js          # Cloud Functions API
│   └── package.json
├── firebase.json          # Firebase 配置
├── firestore.rules        # Firestore 安全規則
├── firestore.indexes.json # Firestore 索引
└── storage.rules          # Storage 安全規則
```

---

## 🔧 常見問題

### 問題 1: Firebase 登入失敗
**解決方案**:
```bash
firebase logout
firebase login
```

### 問題 2: Nuxt 構建失敗
**解決方案**:
```bash
cd frontend-nuxt
rm -rf node_modules .nuxt .output
npm install
npm run build
```

### 問題 3: Functions 部署失敗
**解決方案**:
```bash
cd functions
rm -rf node_modules
npm install
cd ..
firebase deploy --only functions
```

### 問題 4: 權限錯誤
**解決方案**:
- 確保你的 Google 帳號有該 Firebase 專案的管理權限
- 在 Firebase Console 檢查專案設定

---

## 📊 部署後檢查

### 1. 檢查 Hosting
- 訪問你的網站 URL
- 檢查所有頁面是否正常載入

### 2. 檢查 Firestore 規則
```bash
firebase firestore:rules
```

### 3. 檢查 Functions
- 在 Firebase Console 查看 Functions 日誌
- 測試 API 端點

### 4. 監控使用量
- Firebase Console → 使用量和帳單
- 確保在免費額度內

---

## 🎯 部署檢查清單

- [ ] Firebase 登入成功
- [ ] Nuxt 專案構建成功
- [ ] Firestore 規則已部署
- [ ] Storage 規則已部署
- [ ] Hosting 已部署
- [ ] Functions 已部署（可選）
- [ ] Git 變更已提交
- [ ] Git 變更已推送到 GitHub
- [ ] 網站可以正常訪問
- [ ] API 功能測試通過

---

## 📞 需要幫助？

- **Firebase 文檔**: https://firebase.google.com/docs
- **Nuxt 文檔**: https://nuxt.com/docs
- **GitHub Issues**: https://github.com/GALLACE1111/mywebsite/issues

---

## 🔄 更新部署流程

每次更新代碼後：

1. **本地測試**
   ```bash
   cd frontend-nuxt
   npm run dev
   ```

2. **構建並部署**
   ```bash
   npm run build
   cd ..
   firebase deploy --only hosting
   ```

3. **提交到 Git**
   ```bash
   git add .
   git commit -m "你的更新說明"
   git push origin main
   ```

---

## ✅ 完成狀態

- ✅ Git 和 GitHub 配置完成
- ✅ Firebase 配置完成
- ✅ Nuxt 專案構建成功
- ✅ 部署腳本已創建
- ⏳ 等待 Firebase 登入和部署

---

**最後更新**: 2025-11-02
